#include<stdio.h>
#include<string.h>
int main()
{
    int i,len;
    char s[100];
    gets (s);
    for(i=0;s[i]!='\0';i++)
    {

    }
    len=strlen(s);
    for (i=len-1;i>=0;i--)
    {

    }
    printf("%s",s[i]);
    return 0;
}
